const name = "M HASSAN MALIK";  // "UCHIHA ITACHI"; try this name

console.log(name);

/* no  need for browser console you can just check it in terminal by nodeJS by wrting node then file name e.g node test,  and it is going to show on the terminal */
// try changing the name value


// here how you can run function in node
const greet = (dbzName) => {
       console.log(`Hello, ${dbzName}`);
}

greet('Majin Vegeta');
greet('Broly');